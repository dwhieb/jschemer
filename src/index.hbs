{{!-- This is the template for the main page of the schemas documentation --}}

<!DOCTYPE html>
<html lang=en>

  <head>
    <meta charset=utf-8>
    <title>Schemas</title>
    <meta name=viewport content='width=device-width'>
    <link rel=stylesheet href="{{css}}" type=text/css>
  </head>

  <body>

    {{!-- The readme for the documentation --}}
    <main>{{md readme}}</main>

    {{!-- The list of schemas, with links to their individual pages --}}
    <nav>
      <h1>Schemas</h1>
      <ul>
        {{#each nav}}<li><a href="schemas/{{fileName}}">{{title}}</a></li>{{/each}}
      </ul>
    </nav>

  </body>

</html>
