const createCSSFiles    = require(`./createCSSFiles`);
const createHomePage    = require(`./createHomePage`);
const createSchemaPages = require(`./createSchemaPages`);
const getSchemas        = require(`./getSchemas`);

module.exports = {
  createCSSFiles,
  createHomePage,
  createSchemaPages,
  getSchemas,
};
